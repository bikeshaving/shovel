{
  "databases": {
    "main": {
      "dialect": "bun-sqlite",
      "url": "./data/admin.db",
      "schema": "./src/schema.ts"
    }
  },
  "caches": {
    "sessions": {
      "provider": "MODE === production ? redis : memory",
      "url": "REDIS_URL || redis://localhost:6379"
    }
  }
}
